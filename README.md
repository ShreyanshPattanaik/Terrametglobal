# Design Terramet Global Homepage

This is a code bundle for Design Terramet Global Homepage. 

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
