# Design Terramet Global Homepage

This is a code bundle for Design Terramet Global Homepage. The original project is available at https://www.figma.com/design/FOBqWXM2BT7hP5oAzR7Kjk/Design-Terramet-Global-Homepage.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
